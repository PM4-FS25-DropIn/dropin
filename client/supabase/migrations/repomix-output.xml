This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
4. Repository files, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

<additional_info>

</additional_info>

</file_summary>

<directory_structure>
20250411161616_create_profiles_table.sql
20250411161646_create_profiles_rls.sql
20250411161709_create_avatar_bucket.sql
20250411161728_create_avatar_bucket_rls.sql
20250411161743_create_event_thumbnails_bucket.sql
20250411161801_create_event_thumbnails_bucket_rls.sql
20250411161852_create_events_table.sql
20250411161936_create_events_rls.sql
20250411161953_create_event_joins_table.sql
20250411162003_create_event_joins_rls.sql
20250411162202_create_handle_event_update_function_trigger.sql
20250411162225_create_handle_new_user_function_trigger.sql
20250411181539_create_handle_event_creation_function_trigger.sql
20250411183955_create_view_events_not_joined.sql
20250411212032_create_slotsTaken_handlers.sql
20250412155222_create_view_all_joined_events_of_user.sql
20250412234505_create_event_feed_fetch_function.sql
20250501114323_create_messages_tables.sql
20250501114807_create_messages_rls.sql
20250501115201_create_messages_realtime.sql
20250511150100_add_messages_primary_key.sql
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="20250411161616_create_profiles_table.sql">
-- ===========================================
-- Migration:     Create Profiles Table
--
-- Description:
-- Creates the profiles table in the public schema which allows
-- to retrieve data, username and avatar urls easily.  
--  
-- ===========================================
create table public.profiles (
    id uuid references auth.users on delete cascade primary key,
    updated_at timestamp with time zone not null default now(),
    username text not null unique,
    avatar_url text,
    emojicode text,
    city text,
    constraint username check (char_length(username) >= 3)
);

comment on table public.profiles is 'Profiles table of users.';
</file>

<file path="20250411161646_create_profiles_rls.sql">
-- ===========================================
-- Migration:     Create Profiles RLS
--
-- Description:
-- Creates needed RLS policies for the profiles table
-- 
--  
-- ==========================================

alter table profiles enable row level security;

-- Policy for select
create policy "Public profiles are viewable by everyone." on public.profiles
    for select
    to authenticated
    using (true);

-- Policy for insert
create policy "Users can insert their own profile." on public.profiles
    for insert
    to authenticated
    with check
    ((select auth.uid()) = id);

-- Policy for update
create policy "Users can update their own profile." on public.profiles
    for update
    to authenticated
    using ((select auth.uid()) = id);
</file>

<file path="20250411161709_create_avatar_bucket.sql">
-- ===========================================
-- Migration:     Create Avatar Bucket
--
-- Description:
-- Create the storage bucket for the avatar profile pics.
-- 
--  
-- ==========================================

insert into storage.buckets
(id, name, public)
values
('avatars', 'avatars', true);
</file>

<file path="20250411161728_create_avatar_bucket_rls.sql">
-- ===========================================
-- Migration:     Create Avatar Bucket
--
-- Description:
--   - Creates row-level security (RLS) policies for the 'avatars' bucket
--   - Allows authenticated users to publicly read avatar images
--   - Allows authenticated users to upload avatars to their own folder
-- ============================================

create policy "Users can upload avatars." on storage.objects
for insert to authenticated with check (
    bucket_id = 'avatars'
);
</file>

<file path="20250411161743_create_event_thumbnails_bucket.sql">
-- ===========================================
-- Migration:     create_event_bucket
--
-- Description:
--   - Creates a new storage bucket named 'events'
--   - Used for storing images or files related to events
-- ===========================================

insert into storage.buckets
(id, name, public)
values
('event-thumbnails', 'event-thumbnails', true);
</file>

<file path="20250411161801_create_event_thumbnails_bucket_rls.sql">
create policy "Users can upload event thumbnails." on storage.objects
for insert to authenticated with check (
    bucket_id = 'event-thumbnails'
);
</file>

<file path="20250411161852_create_events_table.sql">
-- ===========================================
-- Migration:     create_events_table
--
-- Description:
--   - Creates the 'events' table to store all DropIn events
--   - Defines fields for metadata, location, scheduling, and settings
--   - Includes constraints, default values, and a custom enum 'event_status'
-- ===========================================

-- Events table

create table public.events (
    id bigint generated by default as identity primary key,
    created_at timestamp with time zone default now(),
    updated_at timestamp with time zone default now(),
    title text not null,
    description text not null,
    image_paths text[] not null default array['default.event.thumbnail'],
    user_id uuid references auth.users on delete cascade not null default auth.uid(),
    start timestamp with time zone not null,
    "end" timestamp with time zone not null,
    latitude double precision not null check (latitude >= -90 and latitude <= 90),
    longitude double precision not null check (longitude >= -180 and longitude <= 180),
    slot_limit integer not null check (slot_limit >= 1),
    slots_taken integer not null default 1 check (slots_taken <= slot_limit),
    age_restricted boolean not null default false,
    chat_enabled boolean not null default true
);


comment on table public.events is 'All dropin events';
</file>

<file path="20250411161936_create_events_rls.sql">
-- ===========================================
-- Migration:     create_events_rls
--
-- Description:
--   - Enables row-level security (RLS) on the 'events' table
--   - Prevents banned users from viewing specific events
--   - Allows authenticated users to insert, update, and delete only their own events
-- ===========================================
alter table public.events enable row level security;

create policy "Users can see all events" on public.events
    for select
    to authenticated
    using (
        true
    );

create policy "Users can insert their own events" on public.events
    for insert
    to authenticated
    with check ((select auth.uid()) = user_id);

create policy "Users can update their own events" on public.events
    for update
    to authenticated
    using (
        (select auth.uid()) = user_id
    )
    with check (
        (select auth.uid()) = user_id
    );

create policy "Users can delete their own events" on public.events
    for delete
    to authenticated
    using (
        (select auth.uid()) = user_id
    );
</file>

<file path="20250411161953_create_event_joins_table.sql">
-- ===========================================
-- Migration:     create_event_joins_table
--
-- Description:
--   - Creates the 'event_joins' table to track event participants
--   - Links users to events, includes a timestamp and host flag
--   - Ensures uniqueness of each (event_id, user_id) pair
-- ===========================================
create table public.event_joins (
    id bigint generated always as identity primary key,
    event_id bigint references public.events on delete cascade not null,
    user_id uuid references auth.users on delete cascade not null,
    created_at timestamp with time zone not null default now(),
    is_host boolean not null default false,
    unique(event_id, user_id)
);

comment on table public.event_joins is 'Shows event participants';
</file>

<file path="20250411162003_create_event_joins_rls.sql">
-- ===========================================
-- Migration:     create_event_joins_rls
--
-- Description:
--   - Enables row-level security (RLS) on the 'event_joins' table
--   - Allows users to:
--       • View their own event join records
--       • Leave events they joined
--       • Be auto-added to events via trigger (if user_id matches auth.uid)
-- ===========================================
alter table public.event_joins enable row level security;

create policy "Users can view joined events." on public.event_joins
    for select
    to authenticated
    using (
        (select auth.uid()) = user_id
    );

create policy "Users can leave events." on public.event_joins
    for delete
    to authenticated
    using (
        (select auth.uid()) = user_id
    );

create policy "Users can insert event on autojoin trigger." on public.event_joins
    for insert
    to authenticated
    with check (
       (select auth.uid()) = user_id 
    );
</file>

<file path="20250411162202_create_handle_event_update_function_trigger.sql">
-- ===========================================
-- Migration:     create_handle_event_update_function_trigger
--
-- Description:
--   - Creates a trigger function that automatically updates the
--     'updated_at' timestamp on the 'events' table whenever a row is modified
--   - Trigger runs before any update on the table
-- ===========================================
create function handle_event_update()
returns trigger
set search_path = ''
as $$
begin
    new.updated_at := CURRENT_TIMESTAMP;
    return new;
end;
$$ language plpgsql security definer;
create trigger on_event_update
    before update on public.events
    for each row execute function handle_event_update();
</file>

<file path="20250411162225_create_handle_new_user_function_trigger.sql">
-- ===========================================
-- Migration:     create_handle_new_user_function_trigger
--
-- Description:
--   - Creates a trigger function that inserts a new row into the 'profiles' table
--     when a new user is created in 'auth.users'
--   - Extracts the username from `raw_user_meta_data`
--   - Trigger runs after insert on 'auth.users'
-- ===========================================
create function public.handle_new_user()
returns trigger
set search_path = ''
as $$
begin
  insert into public.profiles (id, username)
  values (new.id, new.raw_user_meta_data->>'username');
  return new;
end;
$$ language plpgsql security definer;
create trigger on_auth_user_created
  after insert on auth.users
  for each row execute function public.handle_new_user();
</file>

<file path="20250411181539_create_handle_event_creation_function_trigger.sql">
-- ===========================================
-- Migration:     create_handle_event_creation_function_trigger
--
-- Description:
--   - Creates a trigger function that automatically adds the event creator
--     to the 'event_joins' table as the host when a new event is created
--   - Trigger runs after insert on the 'events' table
-- ===========================================
create function public.handle_event_creation_autojoin()
returns trigger
set search_path = ''
as $$
begin
	insert into public.event_joins (event_id, user_id, is_host)
	values (new.id, new.user_id, true);
	return new;
end;

$$ language plpgsql security definer;
create trigger on_new_event_created_autojoin
	after insert on public.events
	for each row execute function handle_event_creation_autojoin();
</file>

<file path="20250411183955_create_view_events_not_joined.sql">
-- ===========================================
-- Migration:     create_view_events_not_responded_to
--
-- Description:
--   - Creates a view listing all events the authenticated user
--     has not yet responded to (neither joined nor declined)
--   - Excludes events created by the user themselves
-- ===========================================
create view public.events_not_joined as
select e.* from public.events e 
where e.user_id != auth.uid()
and not exists (
    select 1 
    from public.event_joins ej
    where ej.event_id = e.id and ej.user_id = auth.uid()
);
</file>

<file path="20250411212032_create_slotsTaken_handlers.sql">
-- ===========================================
-- Migration:     create_slotsTaken_handlers
--
-- Description:
--   - Creates trigger functions to update the 'slots_taken' counter
--     on the 'events' table when users join or leave events
--   - 'increment_slots_taken' runs after a new row is added to 'event_joins'
--   - 'decrement_slots_taken' runs after a row is deleted from 'event_joins'
-- ===========================================
create function increment_slots_taken() 
returns trigger
set search_path = ''
as $$
begin
    update public.events
    set slots_taken = slots_taken + 1
    where id = new.event_id;
    return new;
end;
$$ language plpgsql security definer;

create function decrement_slots_taken()
returns trigger
set search_path = ''
as $$
begin
    update public.events
    set slots_taken = slots_taken - 1
    where id = old.event_id;
    return old;
end;
$$ language plpgsql security definer;

create trigger on_new_event_joined
after insert on public.event_joins
for each row
execute function increment_slots_taken();


create trigger on_new_event_leave
after delete on public.event_joins
for each row
execute function decrement_slots_taken();
</file>

<file path="20250412155222_create_view_all_joined_events_of_user.sql">
-- ===========================================
-- Migration:     create_view_all_joined_events_of_user
--
-- Description:
--   - Creates a view 'events_joined_by_user' that lists all events
--     the currently authenticated user has joined
--   - Joins 'events' with 'event_joins' and filters by auth.uid()
-- ===========================================
create view public.events_joined_by_user as 
select e.* from public.events e
join public.event_joins ej on ej.event_id = e.id
where auth.uid() = ej.user_id;
</file>

<file path="20250412234505_create_event_feed_fetch_function.sql">
-- ===========================================
-- Migration:     create_event_feed_fetch_function
--
-- Description:
--   - Creates the 'fetch_events_feed' SQL function
--   - Returns up to 5 events the user has not responded to
--     and filters out any events with IDs in the excluded_ids array
-- ===========================================
create or replace function fetch_events_feed(excluded_ids int[])
returns setof public.events_not_joined
language sql
as $$
    select *
    from public.events_not_joined
    where id != all(excluded_ids)
    limit 5;
$$;
</file>

<file path="20250501114323_create_messages_tables.sql">
-- ===========================================
-- Migration:     create_messages_tables
--
-- Description:
--   This migration creates the messages table.
--   It includes the following columns:
--   - id: UUID, primary key, default value is generated using gen_random_uuid()
--   - sender_id: text, not null
--   - session_name: text, not null
--   - content: text, not null
--   - created_at: timestamp with time zone, not null, default value is the current time in UTC
--   - chat_room_id: bigint
-- ===========================================


create table "public"."messages" (
    "id" uuid not null default gen_random_uuid(),
    "sender_id" text not null,
    "session_name" text not null,
    "content" text not null,
    "created_at" timestamp with time zone not null default timezone('utc'::text, now()),
    "chat_room_id" bigint references public.events on delete cascade not null
);
</file>

<file path="20250501114807_create_messages_rls.sql">
-- ===========================================
-- Migration:     create_messages_rls
--
-- Description:
--   This migration adds row level security policies to the messages table.
-- ===========================================

alter table "public"."messages" enable row level security;

create policy "Anyone can insert messages"
on "public"."messages"
as permissive
for insert
to authenticated, anon
with check (true);


create policy "Anyone can read messages"
on "public"."messages"
as permissive
for select
to authenticated, anon
using (true);
</file>

<file path="20250501115201_create_messages_realtime.sql">
-- ===========================================
-- Migration:     create_messages_realtime
--
-- Description:
--   This migration adds the messages table to the supabase_realtime publication.
-- ===========================================

alter publication supabase_realtime add table messages;
</file>

<file path="20250511150100_add_messages_primary_key.sql">
-- ===========================================
-- Migration:     add_messages_primary_key
--
-- Description:
--   This migration adds a primary key to the messages table.
-- ===========================================

ALTER TABLE messages ADD PRIMARY KEY (id);
</file>

</files>
